# spring-boot-integration-test-jackson-serialization-issue 

Sample spring boot application to demonstrate the `@JsonProperty(access = JsonProperty.Access.READ_ONLY)` issue on controller unit tests.  
